# Diff 驱动一致性建议

> 日期：2026-05-25

## 变更文件

- [TODO: 当前未检测到 git diff 文件，确认是否在 git 仓库中执行。]

## 必须同步项

- 未发现明确的一致性风险。

## 建议命令

- `python scripts/prdctl.py sync . --from-code --prd-root docs/product`
- `python scripts/prdctl.py sync . --from-prd --prd-root docs/product`
- `python scripts/prdctl.py audit . --level strict --prd-root docs/product`
